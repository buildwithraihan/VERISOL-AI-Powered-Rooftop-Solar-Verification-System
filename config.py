# config.py - add real keys before running the pipeline

# Google Static Maps (or other provider). Replace with your API key.
GOOGLE_MAPS_API_KEY = "YOUR_GOOGLE_STATIC_MAPS_API_KEY"

# Default map zoom & image size for satellite fetch
DEFAULT_ZOOM = 20
IMAGE_SIZE = "512x512"

# Output folders (relative to repo root)
OUTPUT_JSON_FOLDER = "Prediction_files"
ARTIFACTS_FOLDER = "Artefacts"

# Temporary folder used during processing
TEMP_FOLDER = "tmp"
